import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  totalMinutesStyle: {
    fontSize:"5rem",
    fontWeight: 500
  },
  noMargin: {
    marginBottom: 0
  }
}));